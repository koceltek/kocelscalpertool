# Deriv application ID registered with this OAuth redirect URL.
VITE_DERIV_APP_ID=your_registered_deriv_app_id
VITE_DERIV_REDIRECT_URI=https://kocelscalpertool.lovable.app/oauth/callback

# Optional comma-separated allowlist; the UI settings select the three active symbols.
VITE_INDICES_ALLOWED_SYMBOLS=R_10,R_25,R_50
